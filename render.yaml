services:
  - type: web
    name: aerova-pro-triage
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: GRADIO_SERVER_PORT
        value: 10000
